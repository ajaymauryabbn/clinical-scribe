# Render backend dependencies
# Audio transcription is done via OpenAI Whisper API (cloud) — no local torch/pyannote needed.
# This keeps the deploy well under Render free tier's 512 MB RAM limit.

# Backend API
fastapi>=0.110.0
uvicorn>=0.29.0
python-multipart>=0.0.9
python-dotenv>=1.0.0

# LLM agent (DeepSeek via OpenAI-compatible SDK)
openai>=1.0.0
langgraph>=0.2.0
langchain>=0.3.0
langchain-openai>=0.2.0

# Audio format conversion (pydub wraps system ffmpeg — no GPU, ~100 KB)
pydub>=0.25.0

# Medical tools
simple_icd_10>=2.0.0
pandas>=2.0.0
rapidfuzz>=3.0.0

# HTTP
requests>=2.28.0

# Testing
pytest>=7.0.0
httpx>=0.24.0
